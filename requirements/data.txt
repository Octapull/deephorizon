# DeepHorizon — data pipeline dependencies
# Install on top of base.txt. Keep isolated from ml.txt — ehtim/astropy
# pin older numpy/scipy that fight torch wheels.

-r base.txt

ehtim>=1.2.10
astropy>=7.0.0
# [s3] extra'si boto3/s3fs getirir; onsuz MinIO remote'una baglanilamaz
# (dvc push -> "missing dependency: dvc-s3"). Bkz. docs/DVC.md
dvc[s3]>=3.59.0
great-expectations>=1.4.0

# NOT: setuptools BURADA PINLENMEZ. pyproject.toml `setuptools<82` diyor (ehtim
# pkg_resources kullaniyor) ama Airflow imajinin constraint dosyasi
# setuptools==82.0.1 pinliyor -> dogrudan cakisma, kurulum coker.
# Imajda setuptools'u Airflow'un kendi kisiti yonetir; ehtim'in gercekten
# calistigi infra/docker/airflow.Dockerfile sonundaki import testiyle dogrulanir.
