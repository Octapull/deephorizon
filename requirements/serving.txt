# DeepHorizon — inference serving dependencies
# Slim runtime image: model + gRPC + Prometheus, no training stack.

-r base.txt

torch>=2.6.0
onnxruntime-gpu>=1.20.0
grpcio>=1.70.0
grpcio-tools>=1.70.0
protobuf>=5.29.0
prometheus-client>=0.22.0
evidently>=0.6.0
