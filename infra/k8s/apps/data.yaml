# Data squad Application: infra/k8s/data/ altindaki kustomize kaynagini izler.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: data
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Octapull/deephorizon.git
    targetRevision: main
    path: infra/k8s/data
  destination:
    server: https://kubernetes.default.svc
    namespace: deephorizon-data
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
